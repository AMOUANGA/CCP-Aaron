<form method="post" action="{{ path('app_option_nom_delete', {'id': option_nom.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ option_nom.id) }}">
    <button class="btn">Delete</button>
</form>
