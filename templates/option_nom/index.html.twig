{% extends 'base.html.twig' %}

{% block title %}OptionNom index{% endblock %}

{% block body %}
    <h1>OptionNom index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for option_nom in option_noms %}
            <tr>
                <td>{{ option_nom.id }}</td>
                <td>{{ option_nom.Nom }}</td>
                <td>{{ option_nom.Prix }}</td>
                <td>
                    <a href="{{ path('app_option_nom_show', {'id': option_nom.id}) }}">show</a>
                    <a href="{{ path('app_option_nom_edit', {'id': option_nom.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_option_nom_new') }}">Create new</a>
{% endblock %}
