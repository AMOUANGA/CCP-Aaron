{% extends 'base.html.twig' %}

{% block title %}OptionNom{% endblock %}

{% block body %}
    <h1>OptionNom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option_nom.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ option_nom.Nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ option_nom.Prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_option_nom_index') }}">back to list</a>

    <a href="{{ path('app_option_nom_edit', {'id': option_nom.id}) }}">edit</a>

    {{ include('option_nom/_delete_form.html.twig') }}
{% endblock %}
