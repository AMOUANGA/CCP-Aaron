{% extends 'base.html.twig' %}

{% block title %}Taille{% endblock %}

{% block body %}
    <h1>Taille</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ taille.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ taille.Nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ taille.Prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_taille_index') }}">back to list</a>

    <a href="{{ path('app_taille_edit', {'id': taille.id}) }}">edit</a>

    {{ include('taille/_delete_form.html.twig') }}
{% endblock %}
